# KubeVision 配置文件
# 敏感信息（密码、JWT Secret）请通过环境变量设置：
# - K8SVISION_AUTH_PASSWORD: bcrypt 哈希后的密码 (60字符)
# - K8SVISION_JWT_SECRET: JWT 签名密钥（建议 64 位以上）

auth:
    username: admin
    password: "$2a$12$rCAp4xtIvVyPyKTWIsI5VObS4uK9ylANFFYlNEnYVoLBhXLp09EAS"

cache:
    enabled: true
    maxsize: 1000
    ttl: 5m
    type: memory

jwt:
    secret: "e8c0dyG8EX/Be0WfImldS9CIWkwgYInb9Ef0uh72d/WC"

kubernetes:
    insecure: false
    kubeconfig: ""

log:
    format: console
    level: info

server:
    host: 0.0.0.0
    port: "8080"

argocd:
  enabled: true
  serverAddr: "argo-cd-argocd-server.argo.svc.cluster.local:443"
  token: ""  # 留空，使用 dynamic client 直接操作 CRD
  insecure: true
  plaintext: false